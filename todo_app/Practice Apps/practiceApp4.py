while True:
    # import builtins  # the builtins module provides access to all the built-in functions and variables in Python.
    # dir(str)
    # help(str.capitalize)
    # This is a simple script that demonstrates the use of strings and user input in Python.  
    # This line is not necessary for the script to function, but it can be used to explore the string class and its methods.
    name = input("What is your name? ")  # Get user input for their name
    print(f"Hello {name.captialize()}")  # Greet the user with their name